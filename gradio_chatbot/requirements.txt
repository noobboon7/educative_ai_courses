gradio>=4.44.0
transformers>=4.42.0
accelerate>=0.33.0
torch>=2.3.0
# sentencepiece is optional and requires CMake to build on macOS/Python 3.13.
# Install later if you use models that need it (e.g., LLaMA/T5/MT5).
safetensors>=0.4.3
numpy>=1.26.0
pydantic>=2.7.0
python-dotenv>=1.0.1
requests>=2.32.0
# Rasa only supports up to Python <3.12 for the 3.x line. Use env markers to avoid install on 3.12+.
# Install in a separate Python 3.10 virtualenv for demos (see requirements-rasa.txt).
rasa>=3.6,<4.0; python_version < "3.12"
rasa-sdk>=3.6,<4.0; python_version < "3.12"
